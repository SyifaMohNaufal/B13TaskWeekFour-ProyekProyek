/* eslint-disable react-native/no-inline-styles */
import React from 'react';
import {Image} from 'react-native';

const Images = () => (
  <Image source={require('../Landing/logo_PP.jpg')} style={{width: 120, height: 120}} />
);

export default Images;
